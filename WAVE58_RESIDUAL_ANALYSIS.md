# Wave58 residual analysis

The recurring-task conditional UI was not primarily a CSS-cache issue. The inline script could become syntactically invalid after template rendering because an escaped newline inside a TypeScript template literal became a literal newline inside a JavaScript single-quoted string. The initial `display:none` remained but change handlers never ran.

The family invitation page had the same class of risk in its generated share text.

## Next
- Audit all rendered inline scripts for escaping/syntax hazards.
- Continue invite lifecycle controls, recurring occurrence exceptions, SQL bind audits, and mobile UI normalization.
