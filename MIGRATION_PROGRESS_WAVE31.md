# Migration Progress Wave31

- [x] イベントAPIを公開ルートから廃止
- [x] イベント登録・編集画面を廃止
- [x] カレンダーからイベント表示を除去
- [x] タスク作成から「関連予定」を除去
- [x] 買い物の関連先をタスクへ一本化
- [x] カレンダー下部の独立登録UIを除去
- [x] LIFFの個別 `next` 遷移を修正
- [x] 既存イベントデータのタスク移行SQLを追加
- [x] Wave30ソースを基準に変更し、index.ts/app.tsを新規設計へ置換していない
- [x] `npx tsc --noEmit` 成功

## Wave31で意図的に残したもの

- `events` / `event_members` テーブル
- 一部のlegacy `event_id` カラム

これは既存データを安全に保持するため。アプリケーションからは参照しない。
