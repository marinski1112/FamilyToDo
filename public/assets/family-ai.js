(()=>{const form=document.getElementById('familyAiForm'),out=document.getElementById('familyAiAnswer');if(!form||!out)return;form.addEventListener('submit',async e=>{e.preventDefault();const q=String(new FormData(form).get('question')||'').trim();if(!q)return;out.textContent='解析中…';const button=form.querySelector('button');button.disabled=true;try{const r=await fetch('/api/family-ai/query',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({question:q})}),d=await r.json();out.textContent=d.answer||d.error||'AI解析を利用できません';}catch{out.textContent='AI解析を利用できません';}finally{button.disabled=false;}});})();
