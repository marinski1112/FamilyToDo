(()=>{
'use strict';
const payload=JSON.parse(document.getElementById('settingsMembersPayload')?.textContent||'{}');const csrf=String(payload.csrf||'');document.querySelectorAll('.member-toggle,.member-del').forEach(b=>b.onclick=async()=>{if(b.classList.contains('member-del')&&!confirm('このメンバーを削除しますか？'))return;const action=b.classList.contains('member-del')?'member_delete':'member_toggle';const r=await fetch('/api/settings',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action,member_id:Number(b.dataset.id),csrf})});const d=await r.json();if(d.ok)location.reload();else alert(d.error||'処理に失敗しました。')});document.querySelectorAll('.invite-revoke').forEach(b=>b.onclick=async()=>{if(!confirm('この招待リンクを取り消しますか？'))return;b.disabled=true;try{const r=await fetch('/api/family/invite',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action:'revoke',id:Number(b.dataset.id),csrf})});const d=await r.json().catch(()=>null);if(!r.ok||!d?.ok)throw new Error(d?.error||'取消に失敗しました。');location.reload();}catch(e){alert(e?.message||String(e));b.disabled=false;}});document.getElementById('invite').onclick=async()=>{const btn=document.getElementById('invite'),out=document.getElementById('inviteOut');btn.disabled=true;out.innerHTML='<div class="meta">招待リンクを作成しています…</div>';try{const r=await fetch('/api/family/invite',{method:'POST',credentials:'same-origin',cache:'no-store',headers:{'content-type':'application/json','accept':'application/json'},body:JSON.stringify({action:'create',csrf,expires_days:7})});const d=await r.json().catch(()=>({ok:false,error:'サーバー応答を読み取れませんでした（HTTP '+r.status+'）'}));if(!r.ok||!d.ok)throw new Error(d.error||'発行に失敗しました。');if(d.ok){const oa=d.official_account;const add=oa?.add_friend_url||'';const nl=String.fromCharCode(10);const shareText='Family TODO LINEへの招待です。'+nl+nl+(add?'① 先に公式アカウントを友だち追加してください'+nl+add+nl+nl:'')+'② 次に家族参加リンクを開いてください'+nl+d.url;document.getElementById('inviteOut').innerHTML='<div class="notice invite-result"><strong>招待リンク</strong><input readonly value="'+d.url.replaceAll('&','&amp;')+'" onclick="this.select()"><div class="meta">有効期限：'+d.expires_at+'</div>'+(add?'<a class="btn line-friend-btn" href="'+add.replaceAll('&','&amp;')+'" target="_blank" rel="noopener noreferrer">公式アカウントを友だち追加</a><input readonly value="'+add.replaceAll('&','&amp;')+'" onclick="this.select()">':'')+'<button type="button" class="btn gray" id="inviteShare">LINE等で共有</button></div>';document.getElementById('inviteShare').onclick=async()=>{try{if(navigator.share)await navigator.share({title:'Family TODO LINE 招待',text:shareText});else{await navigator.clipboard.writeText(shareText);alert('招待文をコピーしました。')}}catch(e){if(e?.name!=='AbortError')alert('共有できませんでした。')}}}}catch(e){out.innerHTML='';alert(e?.message||String(e));}finally{btn.disabled=false}};

document.querySelectorAll('.member-profile-form').forEach(form=>form.addEventListener('submit',async event=>{
  event.preventDefault();
  if(!(form instanceof HTMLFormElement))return;
  const status=form.querySelector('.member-profile-status'),button=form.querySelector('button[type="submit"]'),fd=new FormData(form),memberId=Number(form.dataset.id||0);
  const aiProfilePermissions=[...form.querySelectorAll('input[name="ai_profile_permissions"]:checked')].map(x=>x.value);
  let aiProfilePermissionsOriginal=[];
  try{const parsed=JSON.parse(String(form.dataset.aiPermissionsOriginal||'[]'));if(Array.isArray(parsed))aiProfilePermissionsOriginal=parsed.map(String);}catch{}
  if(button)button.disabled=true;
  if(status)status.textContent='保存しています…';
  try{
    const response=await fetch('/app/settings_members.php',{method:'POST',credentials:'same-origin',cache:'no-store',headers:{'content-type':'application/json','accept':'application/json'},body:JSON.stringify({action:'profile_update',member_id:memberId,birth_date:String(fd.get('birth_date')||''),sex_gender:String(fd.get('sex_gender')||''),birthplace:String(fd.get('birthplace')||''),blood_type:String(fd.get('blood_type')||''),personality_note:String(fd.get('personality_note')||''),ai_personalization_enabled:fd.get('ai_personalization_enabled')==='on',ai_profile_permissions:aiProfilePermissions,ai_profile_permissions_original:aiProfilePermissionsOriginal,csrf})});
    const data=await response.json().catch(()=>null);
    if(!response.ok||!data?.ok)throw new Error(typeof data?.error==='string'?data.error:'プロフィールを保存できませんでした。');
    if(Array.isArray(data.ai_profile_permissions))form.dataset.aiPermissionsOriginal=JSON.stringify(data.ai_profile_permissions.map(String));
    if(status)status.textContent='保存しました。';
  }catch(error){if(status)status.textContent=error instanceof Error?error.message:'プロフィールを保存できませんでした。';}
  finally{if(button)button.disabled=false;}
}));
})();
(()=>{const p98=JSON.parse(document.getElementById('settingsMembersPayload')?.textContent||'{}'),csrf98=String(p98.csrf||'');document.querySelectorAll('.quick-chore-permission').forEach(x=>x.onchange=async()=>{const r=await fetch('/api/settings',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action:'member_permission',member_id:Number(x.dataset.id),granted:x.checked,csrf:csrf98})}),d=await r.json();if(!d.ok){x.checked=!x.checked;alert(d.error||'権限を変更できませんでした。')}});
})();
