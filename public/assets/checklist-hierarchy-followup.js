(()=>{
'use strict';
const parsePayload=id=>{try{return JSON.parse(document.getElementById(id)?.textContent||'{}');}catch{return {};}};
const dailyPayload=parsePayload('dailyPayload'),taskViewPayload=parsePayload('taskViewPayload');
const csrf=String(dailyPayload.csrf||taskViewPayload.csrf||''),U='未分類';
const key=v=>String(v??'').trim().toLocaleLowerCase('ja-JP');
const hide=n=>{if(n instanceof HTMLElement){n.hidden=true;n.classList.add('task-link-ui-hidden');}};
const requestJson=async(url,body)=>{const r=await fetch(url,{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json','accept':'application/json'},body:JSON.stringify(body)}),d=await r.json().catch(()=>({ok:false,error:'サーバー応答を読み取れませんでした。'}));if(!r.ok||!d?.ok)throw new Error(d?.error||'更新に失敗しました。');return d;};

const hideLegacy=()=>{
 document.querySelectorAll('input[name="assignees"],select[name="completion_mode"],.task-shopping-add,.task-shopping-count,.task-shopping,.task-linked-items,.task-linked-shopping').forEach(n=>hide(n.closest?.('[data-assignee-section],.assignee-grid,.assignees-field,.completion-mode-field,fieldset,label')||n));
 document.querySelectorAll('select[name="task_id"]').forEach(n=>hide(n.closest('.shopping-related-task-block,.task-link-section,details,label')));
 document.querySelectorAll('.card h2').forEach(h=>{const t=String(h.textContent||'');if(t.includes('このタスクの買い物')||t.includes('このタスクの持ち物'))hide(h.closest('.card'));});
 document.querySelectorAll('p').forEach(p=>{if(/^担当[：:]/.test(String(p.textContent||'').trim()))hide(p);});
 document.querySelectorAll('.task-row>.meta,.task-child-row>.meta,.unorganized-task-row>.meta').forEach(m=>{m.textContent=String(m.textContent||'').replace(/^\s*[^・]*\s*・\s*/,'').replace(/\s*・\s*担当\s*[^・]+/g,'').trim();});
};

const chooseChildPolicy=count=>new Promise(resolve=>{
 const b=document.createElement('div');b.className='checklist-dialog-backdrop';b.innerHTML=`<div class="checklist-dialog" role="dialog" aria-modal="true"><h2>親タスクを完了しますか？</h2><p>未完了の子タスクが${Number(count)}件あります。</p><button data-v="complete">未完了の子タスクを全て完了にする<small>親・子も完了します</small></button><button data-v="promote">未完了の子タスクを親タスクとして残す<small>子タスクをトップレベルへ移動します</small></button><button data-v="" class="cancel">キャンセル</button></div>`;
 document.body.append(b);const done=v=>{b.remove();resolve(v||null)};b.addEventListener('click',e=>{const x=e.target.closest?.('button[data-v]');if(x)done(x.dataset.v);else if(e.target===b)done(null);});
});
const parentCandidate=t=>{if(!(t instanceof HTMLInputElement)||!t.checked)return null;if(t.id==='done'){const id=Number(taskViewPayload.id||0);return String(taskViewPayload.toggleType||'')==='task'&&id>0?{id,target:t}:null;}if(!t.matches('input.toggle[data-type="task"]')||t.closest('.task-child-row'))return null;const row=t.closest('.task-row[data-task-id],.unorganized-task-row[data-task-id]'),id=Number(row?.dataset.taskId||t.dataset.id||0);return row&&id>0?{id,target:t}:null;};
document.addEventListener('change',e=>{const c=parentCandidate(e.target);if(!c)return;e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();c.target.checked=false;c.target.disabled=true;void(async()=>{try{const i=await requestJson('/api/task-parent-completion',{csrf,id:c.id,action:'inspect'}),n=Number(i.incomplete_children||0);if(n<=0)await requestJson('/api/toggle',{csrf,type:'task',id:c.id,completed:true});else{const p=await chooseChildPolicy(n);if(!p)return;await requestJson('/api/task-parent-completion',{csrf,id:c.id,action:'complete',child_policy:p});}location.reload();}catch(err){alert(err?.message||String(err));}finally{c.target.disabled=false;}})();},true);

const decorateParents=()=>{
 document.querySelectorAll('.task-row[data-task-id],.unorganized-task-row[data-task-id]').forEach(row=>{
  const children=row.querySelector(':scope>.task-children');if(!(children instanceof HTMLElement))return;
  const childRows=[...children.querySelectorAll(':scope>.task-child-row')],composer=children.querySelector(':scope>.task-child-composer');
  row.classList.toggle('task-parent-row',childRows.length>0);children.hidden=false;
  if(composer instanceof HTMLElement){composer.classList.add('task-child-composer-deferred');composer.classList.remove('is-open');let reveal=children.querySelector(':scope>.task-child-add-reveal');if(!(reveal instanceof HTMLButtonElement)){reveal=document.createElement('button');reveal.type='button';reveal.className='task-child-add-reveal';reveal.textContent='＋ 子タスクを追加';composer.before(reveal);reveal.addEventListener('click',()=>{children.hidden=false;reveal.hidden=true;composer.classList.add('is-open');composer.querySelector('input,textarea')?.focus();});}}
  if(!childRows.length)return;
  children.hidden=true;
  let btn=row.querySelector(':scope>.task-main-row .task-child-count-toggle');if(!(btn instanceof HTMLButtonElement)){btn=document.createElement('button');btn.type='button';btn.className='task-child-count-toggle';const actions=row.querySelector(':scope>.task-main-row .checklist-row-actions');(actions||row.querySelector(':scope>.task-main-row'))?.prepend(btn);btn.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();children.hidden=!children.hidden;sync();});}
  const sync=()=>{btn.textContent=`${childRows.length} ${children.hidden?'›':'⌄'}`;btn.setAttribute('aria-expanded',children.hidden?'false':'true');btn.setAttribute('aria-label',`子タスク${childRows.length}件を開閉`);};sync();
 });
};

const installStatusTabs=(section,kind)=>{
 if(!(section instanceof HTMLElement)||section.querySelector(':scope>.checklist-status-tabs'))return;
 const head=section.querySelector(':scope>.section-head');if(!(head instanceof HTMLElement))return;
 const tabs=document.createElement('div');tabs.className='checklist-status-tabs';tabs.setAttribute('role','tablist');tabs.innerHTML='<button type="button" class="active" data-status="pending" role="tab" aria-selected="true">未完了</button><button type="button" data-status="completed" role="tab" aria-selected="false">完了済み</button>';
 head.insertAdjacentElement('afterend',tabs);
 const apply=status=>{section.dataset.statusTab=status;tabs.querySelectorAll('button[data-status]').forEach(b=>{const on=b.dataset.status===status;b.classList.toggle('active',on);b.setAttribute('aria-selected',on?'true':'false');});
  const rows=section.querySelectorAll(kind==='task'?'.task-row,.unorganized-task-row':kind==='shopping'?'.linked-shopping-row,.belongings-category-row':'.belongings-category-row');
  rows.forEach(row=>{if(!(row instanceof HTMLElement))return;const box=row.querySelector('input.toggle');const done=Boolean(box?.checked);row.classList.toggle('checklist-status-hidden',status==='completed'?!done:done);});
  if(kind!=='task')section.querySelectorAll(kind==='shopping'?'.shopping-category-group':'.belongings-category-group').forEach(g=>{if(g instanceof HTMLElement){const visible=[...g.querySelectorAll(kind==='shopping'?'.linked-shopping-row,.belongings-category-row':'.belongings-category-row')].some(r=>r instanceof HTMLElement&&!r.classList.contains('checklist-status-hidden')&&!r.classList.contains('checklist-search-hidden'));g.classList.toggle('checklist-status-group-empty',!visible);}});
 };
 const refreshCounts=()=>{let done=0;const rows=section.querySelectorAll(kind==='task'?'.task-row,.unorganized-task-row':kind==='shopping'?'.linked-shopping-row,.belongings-category-row':'.belongings-category-row');rows.forEach(row=>{if(row.querySelector('input.toggle')?.checked)done++;});const b=tabs.querySelector('[data-status="completed"]');if(b)b.textContent=done?`完了済み ${done}`:'完了済み';};tabs.addEventListener('click',e=>{const b=e.target.closest?.('button[data-status]');if(b)apply(b.dataset.status||'pending');});section.addEventListener('familytodo:toggle-success',()=>requestAnimationFrame(()=>{refreshCounts();apply(section.dataset.statusTab||'pending');}));refreshCounts();apply('pending');
};

const installSearch=section=>{
 const head=section.querySelector(':scope>.section-head');if(!(head instanceof HTMLElement)||head.querySelector('.checklist-search-toggle'))return;
 const tools=document.createElement('div');tools.className='checklist-section-tools';const btn=document.createElement('button');btn.type='button';btn.className='checklist-icon-button checklist-search-toggle';btn.textContent='🔍';btn.setAttribute('aria-label','検索');
 const box=document.createElement('input');box.type='search';box.className='checklist-inline-search';box.placeholder='検索';box.hidden=true;tools.append(btn,box);head.append(tools);
 btn.addEventListener('click',()=>{box.hidden=!box.hidden;if(!box.hidden)box.focus();else{box.value='';box.dispatchEvent(new Event('input'));}});
 box.addEventListener('input',()=>{const q=key(box.value);section.querySelectorAll(':scope>.task-row,:scope>.unorganized-task-row,:scope>.shopping-category-group,:scope>.belongings-category-group').forEach(row=>{if(row instanceof HTMLElement)row.classList.toggle('checklist-search-hidden',Boolean(q)&&!key(row.textContent).includes(q));});});
 return tools;
};

const deleteChoice=(names,kind)=>new Promise(resolve=>{
 const label=names.length===1?`「${names[0]}」カテゴリを削除しますか？`:`${names.length}個のカテゴリを削除しますか？`;
 const noun=kind==='shopping'?'買い物':'持ち物';const b=document.createElement('div');b.className='checklist-dialog-backdrop';b.innerHTML=`<div class="checklist-dialog" role="dialog" aria-modal="true"><h2>${label}</h2><p>カテゴリ内の${noun}の扱いを選んでください。</p><button data-v="unclassified">中の${noun}を未分類に移動して削除</button><button data-v="delete" class="danger">カテゴリと中の${noun}をすべて削除</button><button data-v="" class="cancel">キャンセル</button></div>`;document.body.append(b);const done=v=>{b.remove();resolve(v||null)};b.addEventListener('click',e=>{const x=e.target.closest?.('button[data-v]');if(x)done(x.dataset.v);else if(e.target===b)done(null);});
});

const categoryName=group=>String(group?.dataset?.category||U).trim()||U;
const addDeleteMinus=(head,name,kind,count)=>{
 if(name===U||head.querySelector('.category-delete-minus'))return;const button=document.createElement('button');button.type='button';button.className='category-delete-minus';button.textContent='−';button.setAttribute('aria-label',name+'カテゴリを削除');button.addEventListener('click',async e=>{e.preventDefault();e.stopPropagation();let policy='unclassified';if(Number(count)>0){policy=await deleteChoice([name],kind);if(!policy)return;}try{await requestJson('/api/shopping-category-mutation',{csrf,action:'delete_many',kind,names:[name],item_policy:policy});location.reload();}catch(err){alert(err?.message||String(err));}});head.prepend(button);
};
const renameCategory=async(kind,oldName,node)=>{
 if(!(node instanceof HTMLElement)||node.querySelector(':scope>input.category-inline-rename'))return;
 const original=node.textContent||oldName,input=document.createElement('input');input.type='text';input.className='category-inline-rename';input.value=oldName;input.maxLength=100;input.setAttribute('aria-label','カテゴリ名を編集');node.textContent='';node.append(input);input.focus();input.select();
 let settled=false;const finish=async save=>{if(settled)return;settled=true;const next=String(input.value).trim();if(!save||!next||next===U||key(next)===key(oldName)){node.textContent=original;return;}input.disabled=true;try{if(kind==='shopping')await requestJson('/api/shopping-category-mutation',{csrf,action:'rename',name:oldName,new_name:next});else await requestJson('/api/item',{csrf,action:'category_rename',name:oldName,new_name:next});location.reload();}catch(err){node.textContent=original;alert(err?.message||String(err));}};
 input.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();void finish(true);}else if(e.key==='Escape'){e.preventDefault();void finish(false);}});input.addEventListener('blur',()=>void finish(true),{once:true});
};

const ensureCategoryGroup=(section,kind,name)=>{
 const selector=kind==='shopping'?':scope>.shopping-category-group':':scope>.belongings-category-group';
 let group=[...section.querySelectorAll(selector)].find(g=>g instanceof HTMLElement&&categoryName(g)===name);if(group instanceof HTMLElement)return group;
 if(kind!=='shopping'){const source=document.querySelector('.item-section');if(source instanceof HTMLElement){const g=document.createElement('div');g.className='belongings-category-group shopping-category-group unified-category-group unified-item-group unified-shopping-group category-collapsed';g.dataset.category=name;g.innerHTML=`<div class="belongings-category-head shopping-category-title"><span class="unified-category-icon" aria-hidden="true">🎒</span><span class="belongings-category-name shopping-category-name"></span><button class="belongings-category-toggle shopping-category-toggle" type="button">›</button></div><div class="belongings-category-body"></div><div class="belongings-category-footer shopping-category-footer"><button class="belongings-category-add-item shopping-category-add-item" type="button">＋ 持ち物を追加</button></div>`;g.querySelector('.belongings-category-name').textContent=name;source.append(g);document.dispatchEvent(new CustomEvent('familytodo:category-created',{detail:{kind:'item',name,created_at:''}}));return g;}return null;}
 group=document.createElement('div');group.className='shopping-category-group category-collapsed';group.dataset.category=name;
 const head=document.createElement('div');head.className='shopping-category-title';const label=document.createElement('strong');label.className='shopping-category-name';label.textContent=name;const toggle=document.createElement('button');toggle.type='button';toggle.className='shopping-category-toggle';toggle.textContent='›';toggle.setAttribute('aria-expanded','false');head.append(label,toggle);group.append(head);
 const parking=section.querySelector(':scope>.shopping-category-form-parking');if(parking instanceof HTMLElement)parking.before(group);else section.append(group);return group;
};
const openCategoryComposer=async(section,kind,name)=>{
 const live=ensureCategoryGroup(section,kind,name);if(!(live instanceof HTMLElement))return false;live.hidden=false;live.classList.remove('category-collapsed','checklist-status-group-empty','checklist-search-hidden');
 if(kind==='shopping'){for(let i=0;i<4&&!live.querySelector('.shopping-category-add-item');i++)await new Promise(r=>requestAnimationFrame(r));const trigger=live.querySelector('.shopping-category-add-item');if(!(trigger instanceof HTMLButtonElement))return false;trigger.click();}
 else{const trigger=live.querySelector('.belongings-category-add-item');if(trigger instanceof HTMLButtonElement)trigger.click();const input=live.querySelector('.belongings-composer-name');if(!(input instanceof HTMLElement))return false;requestAnimationFrame(()=>input.focus({preventScroll:false}));}
 live.scrollIntoView({behavior:'smooth',block:'center'});return true;
};

const nextJstOneAt=raw=>{const text=String(raw||'').trim();if(!text)return 0;const created=Date.parse(/Z$|[+-]\d\d:\d\d$/.test(text)?text:text.replace(' ','T')+'Z');if(!Number.isFinite(created))return 0;const local=new Date(created+9*3600000);let target=Date.UTC(local.getUTCFullYear(),local.getUTCMonth(),local.getUTCDate(),1)-9*3600000;if(target<=created)target+=86400000;return target;};
const installCategoryUi=async(section,kind,categories,canManage,categoryMeta=[])=>{
 const metaByKey=new Map((Array.isArray(categoryMeta)?categoryMeta:[]).map(row=>[key(row?.name),row]));
 const emptyEligible=name=>{const meta=metaByKey.get(key(name));if(!meta)return true;const at=nextJstOneAt(meta.created_at);return !at||Date.now()>=at;};
 const rememberCreated=e=>{const d=e?.detail;if(!d||d.kind!==kind||!d.name)return;metaByKey.set(key(d.name),{name:String(d.name),created_at:String(d.created_at||'')});const g=[...section.querySelectorAll(kind==='shopping'?'.shopping-category-group':'.belongings-category-group')].find(x=>key(x.dataset.category)===key(d.name));if(g instanceof HTMLElement){g.dataset.createdAt=String(d.created_at||'');g.hidden=false;g.classList.remove('checklist-status-group-empty','checklist-search-hidden');}};
 document.addEventListener('familytodo:category-created',rememberCreated);
 if(section.dataset.categoryContractDelegated!=='1'){section.dataset.categoryContractDelegated='1';section.addEventListener('click',e=>{const node=e.target.closest?.(kind==='shopping'?'.shopping-category-name':'.belongings-category-name');if(!(node instanceof HTMLElement)||node.closest('.category-delete-mode-active'))return;const group=node.closest(kind==='shopping'?'.shopping-category-group':'.belongings-category-group'),name=categoryName(group);if(name===U)return;e.preventDefault();e.stopPropagation();void renameCategory(kind,name,node);},true);}
 const tools=installSearch(section)||section.querySelector(':scope>.section-head .checklist-section-tools');if(!(tools instanceof HTMLElement))return;
 const existingAdd=[...section.querySelectorAll('button')].find(b=>/カテゴリ/.test(String(b.textContent||''))&&/(追加|\＋|\+)/.test(String(b.textContent||'')));
 if(existingAdd instanceof HTMLButtonElement){existingAdd.classList.add('checklist-compact-action');existingAdd.textContent='＋ カテゴリ';tools.append(existingAdd);}
 section.querySelector(':scope>.belongings-category-toolbar')?.remove();
 const selected=new Set(),trash=document.createElement('button');trash.type='button';trash.className='checklist-icon-button category-delete-mode';trash.textContent='🗑️';trash.setAttribute('aria-label','カテゴリ削除');trash.title='カテゴリ削除';if(canManage)tools.insertBefore(trash,tools.firstChild);
 const groups=[...section.querySelectorAll(kind==='shopping'?':scope>.shopping-category-group':':scope>.belongings-category-group')].filter(g=>g instanceof HTMLElement);
 const active=new Set();
 for(const g of groups){const name=categoryName(g),head=g.querySelector(kind==='shopping'?':scope>.shopping-category-title':':scope>.belongings-category-head');const rows=g.querySelectorAll(kind==='shopping'?':scope>.linked-shopping-row':':scope>.belongings-category-body>.belongings-category-row');const count=rows.length;if(count&&name!==U)active.add(key(name));
  if(head instanceof HTMLElement){const nameNode=head.querySelector(kind==='shopping'?'.shopping-category-name':'.belongings-category-name');if(nameNode instanceof HTMLElement&&name!==U){nameNode.classList.add('category-name-editable');nameNode.title='カテゴリ名を編集';}
   let countBtn=head.querySelector('.checklist-category-count-toggle');if(!(countBtn instanceof HTMLButtonElement)){countBtn=document.createElement('button');countBtn.type='button';countBtn.className='checklist-category-count-toggle';const native=head.querySelector(kind==='shopping'?'.shopping-category-toggle':'.belongings-category-toggle');head.insertBefore(countBtn,native||null);countBtn.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();const n=head.querySelector(kind==='shopping'?'.shopping-category-toggle':'.belongings-category-toggle');if(n instanceof HTMLButtonElement)n.click();});}countBtn.textContent=String(count);}
  if(!count)g.hidden=name===U?false:emptyEligible(name);else if(name===U){g.hidden=false;g.classList.remove('checklist-status-group-empty','checklist-search-hidden');}
 }
 const zero=categories.filter(n=>n&&n!==U&&!active.has(key(n))&&emptyEligible(n));let cluster=section.querySelector(':scope>.zero-category-cluster');cluster?.remove();
 if(zero.length){cluster=document.createElement('details');cluster.className='zero-category-cluster';cluster.innerHTML=`<summary><span>空のカテゴリ</span><span>${zero.length}</span></summary><div class="zero-category-cluster-list"></div>`;const list=cluster.querySelector('.zero-category-cluster-list');zero.forEach(name=>{const row=document.createElement('div');row.className='zero-category-cluster-row';const title=document.createElement('button');title.type='button';title.className='zero-category-name';title.textContent=name;title.title='カテゴリ名を編集';title.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();void renameCategory(kind,name,title);});const addTo=document.createElement('button');addTo.type='button';addTo.className='zero-category-add-item';addTo.textContent='＋ 追加';addTo.setAttribute('aria-label',`${name}に${kind==='shopping'?'買い物':'持ち物'}を追加`);addTo.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();void openCategoryComposer(section,kind,name);});row.append(title,addTo);list.append(row);});section.append(cluster);}const add=document.createElement('button');add.type='button';add.className='zero-unclassified-add';add.textContent=kind==='shopping'?'＋ 未分類に買い物を追加':'＋ 未分類に持ち物を追加';add.addEventListener('click',()=>{void openCategoryComposer(section,kind,U);});section.append(add);
 const setMode=on=>{section.classList.toggle('category-delete-mode-active',on);document.querySelectorAll('.category-delete-minus').forEach(n=>n.remove());if(on){groups.forEach(g=>{const n=categoryName(g),h=g.querySelector(kind==='shopping'?':scope>.shopping-category-title':':scope>.belongings-category-head'),count=g.querySelectorAll(kind==='shopping'?':scope>.linked-shopping-row':':scope>.belongings-category-body>.belongings-category-row').length;if(h instanceof HTMLElement&&n!==U){g.hidden=false;addDeleteMinus(h,n,kind,count);}});cluster?.querySelectorAll('.zero-category-cluster-row').forEach(row=>{const n=String(row.querySelector('.zero-category-name')?.textContent||'');if(n)addDeleteMinus(row,n,kind,0);});if(cluster instanceof HTMLDetailsElement)cluster.open=true;}};
 trash.addEventListener('click',()=>{const on=!section.classList.contains('category-delete-mode-active');setMode(on);trash.textContent=on?'✕':'🗑️';trash.setAttribute('aria-label',on?'削除モードを終了':'削除');trash.classList.toggle('text-button',on);});
};


const installUnifiedChecklist=()=>{
 const task=document.querySelector('.task-section'),shopping=document.querySelector('.shopping-checklist-section'),items=document.querySelector('.item-section');
 if(task instanceof HTMLElement){
  const head=task.querySelector(':scope>.section-head'),tools=head?.querySelector('.checklist-section-tools'),status=task.querySelector(':scope>.checklist-status-tabs');
  head?.querySelector('h2')?.classList.add('unified-original-title-hidden');
  head?.querySelector('.checklist-search-toggle')?.remove();head?.querySelector('.checklist-inline-search')?.remove();
  const events=[...task.querySelectorAll(':scope>.event-task-row')],eventCount=events.length;
  const tabs=document.createElement('div');tabs.className='checklist-kind-tabs task-event-tabs';tabs.innerHTML=`<button type="button" class="active" data-kind="task">☑ タスク</button><button type="button" data-kind="event">📅 イベント${eventCount?` <span>${eventCount}</span>`:''}</button>`;head?.prepend(tabs);
  const add=tools?.querySelector('.task-add-top');if(add instanceof HTMLElement){add.dataset.inputKind='task';if(add instanceof HTMLAnchorElement)add.dataset.baseHref=add.href;}
  const ai=document.createElement('a');ai.className='checklist-compact-action unified-ai-input';ai.textContent='＋AI入力';ai.href='/task/new.php?date='+encodeURIComponent(String(dailyPayload.date||''))+'&type=task';
  if(tools instanceof HTMLElement){tools.prepend(ai);const trash=tools.querySelector('.task-delete-mode');if(trash instanceof HTMLElement&&status instanceof HTMLElement)status.append(trash);}
  const apply=kind=>{task.dataset.kindTab=kind;const liveAdd=task.querySelector('.task-add-top');tabs.querySelectorAll('button').forEach(b=>b.classList.toggle('active',b.dataset.kind===kind));task.querySelectorAll(':scope>.task-row').forEach(row=>{if(!(row instanceof HTMLElement))return;const isEvent=row.classList.contains('event-task-row');row.classList.toggle('checklist-kind-hidden',kind==='event'?!isEvent:isEvent);});task.querySelectorAll(':scope>.unorganized-task-row').forEach(row=>row.classList.toggle('checklist-kind-hidden',kind==='event'));if(liveAdd instanceof HTMLElement){liveAdd.textContent=kind==='event'?'＋ イベント':'＋ タスク';liveAdd.dataset.inputKind=kind;const input=task.querySelector('.section-quick-name');if(input){input.placeholder=kind==='event'?'新しいイベント':'新しいタスク';input.setAttribute('aria-label',input.placeholder);}if(liveAdd instanceof HTMLAnchorElement){const u=new URL(liveAdd.dataset.baseHref||liveAdd.href,location.href);u.searchParams.set('type',kind);if(kind==='event')u.searchParams.set('event','1');else u.searchParams.delete('event');liveAdd.href=u.pathname+u.search;}}if(ai instanceof HTMLAnchorElement){const u=new URL(ai.href,location.href);u.searchParams.set('type',kind);if(kind==='event')u.searchParams.set('event','1');else u.searchParams.delete('event');ai.href=u.pathname+u.search;}};
  tabs.addEventListener('click',e=>{const b=e.target.closest?.('button[data-kind]');if(b)apply(b.dataset.kind||'task');});apply('task');
 }
 if(shopping instanceof HTMLElement&&items instanceof HTMLElement){
  shopping.classList.add('unified-goods-section');items.classList.add('unified-items-source');
  const head=shopping.querySelector(':scope>.section-head'),tools=head?.querySelector('.checklist-section-tools'),status=shopping.querySelector(':scope>.checklist-status-tabs');
  head?.querySelector('h2')?.classList.add('unified-original-title-hidden');head?.querySelector('.checklist-search-toggle')?.remove();head?.querySelector('.checklist-inline-search')?.remove();
  items.querySelector(':scope>.section-head')?.querySelector('.checklist-search-toggle')?.remove();
  const tabs=document.createElement('div');tabs.className='checklist-kind-tabs goods-kind-tabs';tabs.innerHTML='<button type="button" class="active" data-kind="shopping">🛒 買い物</button><button type="button" data-kind="item">🎒 持ち物</button>';head?.prepend(tabs);
  const ai=document.createElement('a');ai.className='checklist-compact-action unified-ai-input';ai.textContent='＋AI入力';ai.href='/task/new.php?date='+encodeURIComponent(String(dailyPayload.date||''))+'&type=shopping';
  if(tools instanceof HTMLElement)tools.prepend(ai);
  const shoppingGroups=[...shopping.querySelectorAll(':scope>.shopping-category-group')],itemGroups=[...items.querySelectorAll(':scope>.belongings-category-group')];
  shoppingGroups.forEach(g=>{g.classList.add('unified-category-group','unified-shopping-group');const h=g.querySelector(':scope>.shopping-category-title');if(h&&!h.querySelector('.unified-category-icon'))h.insertAdjacentHTML('afterbegin','<span class="unified-category-icon" aria-hidden="true">🛒</span>');});
  itemGroups.forEach(g=>{g.classList.add('unified-category-group','unified-item-group','shopping-category-group','unified-shopping-group');const h=g.querySelector(':scope>.belongings-category-head');if(h)h.classList.add('shopping-category-title');const n=h?.querySelector('.belongings-category-name');if(n)n.classList.add('shopping-category-name');const t=h?.querySelector('.belongings-category-toggle');if(t)t.classList.add('shopping-category-toggle');if(h&&!h.querySelector('.unified-category-icon'))h.insertAdjacentHTML('afterbegin','<span class="unified-category-icon" aria-hidden="true">🎒</span>');if(h&&!h.querySelector('.checklist-category-count-toggle')){const count=document.createElement('button');count.type='button';count.className='checklist-category-count-toggle';count.textContent=String(g.querySelectorAll(':scope>.belongings-category-body>.belongings-category-row').length);const toggle=h.querySelector('.belongings-category-toggle');h.insertBefore(count,toggle||null);count.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();const t=h.querySelector('.belongings-category-toggle');if(t instanceof HTMLButtonElement)t.click();});}shopping.append(g);});
  const itemZero=items.querySelector(':scope>.zero-category-cluster');if(itemZero){itemZero.classList.add('unified-item-zero');shopping.append(itemZero);}
  const shoppingUn=shopping.querySelector(':scope>.zero-unclassified-add'),itemUn=items.querySelector(':scope>.zero-unclassified-add');if(itemUn)shopping.append(itemUn);
  const itemStatus=items.querySelector(':scope>.checklist-status-tabs');itemStatus?.remove();
  const itemHead=items.querySelector(':scope>.section-head');if(itemHead)itemHead.hidden=true;items.hidden=true;
  const itemCategoryAdd=[...items.querySelectorAll('.belongings-add-category,.checklist-compact-action')].find(b=>/カテゴリ/.test(String(b.textContent||'')));if(itemCategoryAdd instanceof HTMLElement){itemCategoryAdd.dataset.inputKind='item';tools?.append(itemCategoryAdd);}
  const shoppingCategoryAdd=[...shopping.querySelectorAll('.shopping-category-add,.checklist-compact-action')].find(b=>/カテゴリ/.test(String(b.textContent||''))&&b!==itemCategoryAdd);if(shoppingCategoryAdd instanceof HTMLElement)shoppingCategoryAdd.dataset.inputKind='shopping';
  const shoppingTrash=tools?.querySelector('.category-delete-mode'),itemTrash=items.querySelector('.category-delete-mode');if(itemTrash instanceof HTMLElement)tools?.append(itemTrash);
  const catButtons=[shoppingCategoryAdd,itemCategoryAdd].filter(Boolean),trashButtons=[shoppingTrash,itemTrash].filter(Boolean);
  let active='shopping';let apply=kind=>{active=kind;shopping.classList.remove('category-delete-mode-active');items.classList.remove('category-delete-mode-active');shopping.querySelectorAll('.category-delete-minus').forEach(n=>n.remove());for(const b of trashButtons){if(b instanceof HTMLElement){b.textContent='🗑️';b.classList.remove('text-button');}}shopping.dataset.inputKind=kind;tabs.querySelectorAll('button').forEach(b=>b.classList.toggle('active',b.dataset.kind===kind));shopping.querySelectorAll(':scope>.unified-category-group').forEach(g=>{if(g instanceof HTMLElement)g.hidden=(kind==='item')?!g.classList.contains('unified-item-group'):g.classList.contains('unified-item-group');});const sz=shopping.querySelector(':scope>.zero-category-cluster:not(.unified-item-zero)'),iz=shopping.querySelector(':scope>.zero-category-cluster.unified-item-zero');if(sz instanceof HTMLElement)sz.hidden=kind!=='shopping';if(iz instanceof HTMLElement)iz.hidden=kind!=='item';catButtons.forEach(b=>{if(!(b instanceof HTMLElement))return;b.hidden=b.dataset.inputKind!==kind;});trashButtons.forEach(b=>{if(!(b instanceof HTMLElement))return;const owns=b===itemTrash?'item':'shopping';b.hidden=owns!==kind;});if(shoppingUn instanceof HTMLElement)shoppingUn.hidden=kind!=='shopping';if(itemUn instanceof HTMLElement)itemUn.hidden=kind!=='item';if(ai instanceof HTMLAnchorElement){const u=new URL(ai.href,location.href);u.searchParams.set('type',kind);ai.href=u.pathname+u.search;}};
  tabs.addEventListener('click',e=>{const b=e.target.closest?.('button[data-kind]');if(b)apply(b.dataset.kind||'shopping');});
  if(shoppingTrash instanceof HTMLElement&&status instanceof HTMLElement)status.append(shoppingTrash);
  apply('shopping');
 }
};

const waitFor=async test=>{for(let i=0;i<120;i++){const v=test();if(v)return v;await new Promise(r=>requestAnimationFrame(r));}return test();};
const setup=async()=>{
 if(location.pathname!=='/app/tasks.php')return;hideLegacy();decorateParents();
 const task=document.querySelector('.task-section');if(task instanceof HTMLElement){installStatusTabs(task,'task');installSearch(task);const head=task.querySelector(':scope>.section-head'),toolbars=head?[...head.querySelectorAll(':scope>.checklist-section-tools')]:[],tools=toolbars[0];if(tools instanceof HTMLElement)for(const extra of toolbars.slice(1)){while(extra.firstChild)tools.append(extra.firstChild);extra.remove();}const add=[...task.querySelectorAll('a,button')].find(n=>/タスクを追加|＋\s*タスク/.test(String(n.textContent||'').trim())&&!/子タスク/.test(String(n.textContent||'')));if(add instanceof HTMLElement&&tools instanceof HTMLElement){add.classList.add('checklist-compact-action','task-add-top');add.textContent='＋ タスク';tools.append(add);}if(tools instanceof HTMLElement){const trash=document.createElement('button');trash.type='button';trash.className='checklist-icon-button task-delete-mode';trash.textContent='🗑️';trash.setAttribute('aria-label','タスク削除');tools.prepend(trash);const search=tools.querySelector('.checklist-search-toggle'),taskAdd=tools.querySelector('.task-add-top');if(search instanceof HTMLElement)trash.after(search);if(taskAdd instanceof HTMLElement)tools.append(taskAdd);const setTaskDeleteMode=on=>{task.classList.toggle('task-delete-mode-active',on);task.querySelectorAll('.task-delete-minus').forEach(n=>n.remove());if(on)task.querySelectorAll(':scope>.task-row[data-task-id],:scope>.unorganized-task-row[data-task-id]').forEach(row=>{const id=Number(row.dataset.taskId||0);if(id<=0)return;const main=row.querySelector(':scope>.task-main-row');if(!(main instanceof HTMLElement))return;const minus=document.createElement('button');minus.type='button';minus.className='task-delete-minus';minus.textContent='−';minus.setAttribute('aria-label','このタスクを削除');minus.addEventListener('click',async e=>{e.preventDefault();e.stopPropagation();const childCount=row.querySelectorAll(':scope>.task-children>.task-child-row').length;if(childCount&&!confirm('子タスクも含めてこのタスクを削除しますか？'))return;try{const r=await fetch('/api/task?id='+encodeURIComponent(String(id)),{method:'DELETE',credentials:'same-origin',headers:{'x-csrf':csrf,'accept':'application/json'}}),d=await r.json().catch(()=>({ok:false,error:'削除結果を確認できませんでした。'}));if(!r.ok||!d.ok)throw new Error(d.error||'削除に失敗しました。');row.remove();}catch(err){alert(err?.message||String(err));}});main.prepend(minus);});};trash.addEventListener('click',()=>{const on=!task.classList.contains('task-delete-mode-active');setTaskDeleteMode(on);trash.textContent=on?'✕':'🗑️';trash.setAttribute('aria-label',on?'削除モードを終了':'削除');trash.classList.toggle('text-button',on);});}}
 const shopping=document.querySelector('.shopping-checklist-section'),items=document.querySelector('.item-section');
 let sc={categories:[],canManageCategories:false},ic={categories:[]};try{sc=await (await fetch('/api/shopping-categories',{credentials:'same-origin',cache:'no-store'})).json();}catch{}try{ic=await (await fetch('/api/item?view=categories',{credentials:'same-origin',cache:'no-store'})).json();}catch{}
 const mergedCategories=[],mergedMeta=[],seenCategories=new Set(),seenMeta=new Set();for(const name of [...(Array.isArray(sc.categories)?sc.categories:[]),...(Array.isArray(ic.categories)?ic.categories:[])]){const n=String(name||'').trim(),k=key(n);if(n&&!seenCategories.has(k)){seenCategories.add(k);mergedCategories.push(n);}}for(const row of [...(Array.isArray(sc.categoryMeta)?sc.categoryMeta:[]),...(Array.isArray(ic.categoryMeta)?ic.categoryMeta:[])]){const n=String(row?.name||'').trim(),k=key(n);if(!n||seenMeta.has(k))continue;seenMeta.add(k);mergedMeta.push({name:n,created_at:String(row?.created_at||'')});}
 if(shopping instanceof HTMLElement){await waitFor(()=>shopping.querySelector('.shopping-category-group,.shopping-category-add'));installStatusTabs(shopping,'shopping');await installCategoryUi(shopping,'shopping',mergedCategories,Boolean(sc.canManageCategories),mergedMeta);}
 if(items instanceof HTMLElement){await waitFor(()=>items.querySelector('.belongings-category-group,.belongings-add-category'));installStatusTabs(items,'item');await installCategoryUi(items,'item',mergedCategories,Boolean(sc.canManageCategories),mergedMeta);}
 installUnifiedChecklist();
 document.dispatchEvent(new CustomEvent('familytodo:checklist-unified-ready'));
};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>{setTimeout(()=>void setup(),0)},{once:true});else setTimeout(()=>void setup(),0);
})();