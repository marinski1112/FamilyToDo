(()=>{'use strict';const boot=()=>{if(location.pathname!=='/app/tasks.php')return;const goods=document.querySelector('.unified-goods-section');if(!(goods instanceof HTMLElement))return;const payload=(()=>{try{return JSON.parse(document.getElementById('dailyPayload')?.textContent||'{}')}catch{return {}}})();const date=new URLSearchParams(location.search).get('date')||String(payload.date||'');const post=async b=>{const r=await fetch('/api/shopping',{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json','accept':'application/json'},body:JSON.stringify({csrf:String(payload.csrf||''),...b})}),d=await r.json().catch(()=>({ok:false}));if(!r.ok||!d.ok)throw new Error(d.error||'更新に失敗しました。');return d};const button=document.createElement('button');button.type='button';button.className='belongings-reusable-set-button shopping-reusable-set-button';button.textContent='セット';button.dataset.inputKind='shopping';const status=goods.querySelector('.checklist-status-tabs'),trash=status?.querySelector('.category-delete-mode');status?.insertBefore(button,trash||null);const sync=()=>button.hidden=goods.dataset.inputKind==='item';sync();document.querySelector('.goods-kind-tabs')?.addEventListener('click',()=>requestAnimationFrame(sync));const overlay=document.createElement('div');overlay.className='belongings-set-overlay';overlay.hidden=true;overlay.innerHTML='<div class="belongings-set-backdrop"></div><section class="belongings-set-sheet" role="dialog" aria-modal="true"><div class="belongings-set-head"><strong>買い物セット</strong><button class="belongings-set-close" type="button" aria-label="閉じる">×</button></div><p class="belongings-set-note">現在の買い物をセットとして保存し、必要な日にまとめて追加できます。</p><button class="belongings-set-save-current" type="button">表示中すべてからセット保存</button><div class="belongings-set-list"></div><div class="belongings-set-status" role="status"></div></section>';document.body.append(overlay);const list=overlay.querySelector('.belongings-set-list'),msg=overlay.querySelector('.belongings-set-status');const close=()=>overlay.hidden=true;overlay.querySelector('.belongings-set-close')?.addEventListener('click',close);overlay.querySelector('.belongings-set-backdrop')?.addEventListener('click',close);const load=async()=>{const r=await fetch('/api/shopping?view=reusable_sets',{credentials:'same-origin',headers:{accept:'application/json'}}),d=await r.json();if(!r.ok||!d.ok)throw new Error(d.error||'セットを読み込めませんでした。');list.replaceChildren();for(const s of d.sets||[]){const row=document.createElement('div');row.className='belongings-set-row';row.innerHTML='<div class="belongings-set-meta"><strong></strong><span></span></div><div class="belongings-set-actions"></div>';row.querySelector('strong').textContent=String(s.name||'');row.querySelector('span').textContent=String(s.item_count||0)+'件';const add=document.createElement('button');add.className='belongings-set-add';add.textContent='追加';add.onclick=async()=>{add.disabled=true;try{await post({action:'reusable_set_invoke',set_id:Number(s.id),date,client_request_id:crypto.randomUUID()});location.reload()}catch(e){msg.textContent=e.message}finally{add.disabled=false}};row.querySelector('.belongings-set-actions').append(add);if(s.can_delete){const del=document.createElement('button');del.className='belongings-set-delete';del.textContent='削除';del.onclick=async()=>{if(!confirm('「'+s.name+'」を削除しますか？'))return;await post({action:'reusable_set_delete',set_id:Number(s.id)});await load()};row.querySelector('.belongings-set-actions').append(del)}list.append(row)}if(!(d.sets||[]).length)list.textContent='保存済みセットはありません。'};button.onclick=()=>{overlay.hidden=false;void load().catch(e=>msg.textContent=e.message)};overlay.querySelector('.belongings-set-save-current').onclick=async e=>{const source=[...goods.querySelectorAll('.unified-shopping-group input.toggle[data-type="shopping"][data-id]')].map(x=>Number(x.dataset.id)).filter(Boolean);if(!source.length){msg.textContent='保存できる買い物がありません。';return}const name=String(prompt('セット名（例：週末の買い物）','')||'').trim();if(!name)return;e.currentTarget.disabled=true;try{await post({action:'reusable_set_create',name,source_item_ids:source});await load();msg.textContent='保存しました。'}catch(err){msg.textContent=err.message}finally{e.currentTarget.disabled=false}}};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();})();