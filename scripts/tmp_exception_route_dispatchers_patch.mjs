import fs from 'node:fs';

const indexPath='src/index.ts';
const modulePath='src/exception-routes.ts';
const contractPath='scripts/exception-route-dispatchers-contract.mjs';
const manifestPath='scripts/regression-manifest.mjs';

const replaceOnce=(source,from,to,label)=>{
  const first=source.indexOf(from);
  if(first<0) throw new Error(`missing ${label}`);
  if(source.indexOf(from,first+from.length)>=0) throw new Error(`duplicate ${label}`);
  return source.slice(0,first)+to+source.slice(first+from.length);
};

let index=fs.readFileSync(indexPath,'utf8');

const prelude=`      if(url.pathname==='/oauth/google/authorize') {\n        console.log(JSON.stringify({stage:'AUTHORIZE_RECEIVED',provider:'GOOGLE_HOME'}));\n        return await preserveGoogleHomeLogin(request,env,await googleAuthorize(request,context));\n      }\n      if(url.pathname==='/oauth/google-tasks/authorize') return await googleTasksAuthorize(request,context);\n      if(url.pathname==='/oauth/google-calendar/authorize') return await googleCalendarAuthorize(request,context);\n      if(url.pathname==='/app/api/liff_login.php'||url.pathname==='/app/api/liff_login') return await liffLogin(request,context);\n`;
const preludeCall=`      const preludeResponse=await dispatchContextPreludeRoute(request,context,env,url);\n      if(preludeResponse) return preludeResponse;\n`;
index=replaceOnce(index,prelude,preludeCall,'context prelude routes');

const fallback=`      if(url.pathname==='/app/api/liff_config_diagnose.php'||url.pathname==='/app/api/liff_config_diagnose') return await liffConfigDiagnose(env);\n      if(url.pathname==='/app/api/check.php'||url.pathname==='/app/api/check') return await toggle(request,context);\n      if(url.pathname==='/app/api/reorder.php'||url.pathname==='/app/api/reorder') return await reorderApi(request,context);\n      if(url.pathname==='/webhook'||url.pathname==='/app/api/webhook'||url.pathname==='/app/api/webhook.php') return await webhook(request,env);\n      if(url.pathname==='/logout.php'||url.pathname==='/logout') return await logout(request,env);\n      if(url.pathname==='/task/delete.php') return await taskDelete(request,context);\n      if(url.pathname==='/task/convert_occurrence.php') return await convertOccurrence(request,context);\n      if(url.pathname==='/task/new.php') return await taskNew(context,url.searchParams.get('date')||asDateOffset(0,String(context.member?.family_timezone||env.APP_TIMEZONE||DEFAULT_FAMILY_TIMEZONE)),url.searchParams.get('return')||'');\n      if(url.pathname==='/item/new.php') return await itemNew(context,url.searchParams.get('date')||asDateOffset(0,String(context.member?.family_timezone||env.APP_TIMEZONE||DEFAULT_FAMILY_TIMEZONE)),Number(url.searchParams.get('task_id')||0));\n`;
const fallbackCall=`      const fallbackResponse=await dispatchContextFallbackRoute(request,context,env,url);\n      if(fallbackResponse) return fallbackResponse;\n`;
index=replaceOnce(index,fallback,fallbackCall,'context fallback routes');

const logoutStart=index.indexOf('async function logout(');
if(logoutStart<0) throw new Error('logout handler missing');
index=index.slice(0,logoutStart).trimEnd()+'\n';

const asDateLine="function asDateOffset(days:number,timeZone=DEFAULT_FAMILY_TIMEZONE){const base=familyDate(timeZone),d=new Date(`${base}T12:00:00Z`);d.setUTCDate(d.getUTCDate()+days);return d.toISOString().slice(0,10);}\n\n";
index=replaceOnce(index,asDateLine,'','asDateOffset helper');

index=replaceOnce(index,"import { makeContext, liffLogin,","import { makeContext,",'liffLogin app import');
index=replaceOnce(index,", shopping, toggle, home,",", shopping, home,",'toggle app import');
index=replaceOnce(index,"import { googleAuthorize, googleFulfillment, googleHomeHealth, googleHomeSettings, googleToken } from './google-home';","import { googleFulfillment, googleHomeHealth, googleHomeSettings, googleToken } from './google-home';",'google authorize import');
index=replaceOnce(index,"import { googleTasksAuthorize, googleTasksCallback, googleTasksSettings, googleTasksAction, processGoogleTasksInbound } from './google-tasks';","import { googleTasksCallback, googleTasksSettings, googleTasksAction, processGoogleTasksInbound } from './google-tasks';",'google tasks authorize import');
index=replaceOnce(index,"import { googleCalendarAuthorize, googleCalendarCallback, integrationsSettings, queueCalendarProjectionAfterMutation, processCalendarOutbox, processCalendarInbound, calendarSyncNow, calendarDisconnect, calendarRetryFailed, calendarBackfill, calendarWatchWebhook, renewCalendarWatches, wakeCalendarOutbox } from './google-calendar';","import { googleCalendarCallback, integrationsSettings, queueCalendarProjectionAfterMutation, processCalendarOutbox, processCalendarInbound, calendarSyncNow, calendarDisconnect, calendarRetryFailed, calendarBackfill, calendarWatchWebhook, renewCalendarWatches, wakeCalendarOutbox } from './google-calendar';",'google calendar authorize import');
index=replaceOnce(index,"import { DEFAULT_FAMILY_TIMEZONE, familyDate, formatStoredUtcForFamily, utcNow } from './timezone';","import { formatStoredUtcForFamily, utcNow } from './timezone';",'new page timezone imports');
index=replaceOnce(index,"import { preserveGoogleHomeLogin, liffDispatcher, resumeGoogleHome, lineGoogleHomeStart, lineGoogleHomeCallback } from './oauth-continuation';","import { liffDispatcher, resumeGoogleHome, lineGoogleHomeStart, lineGoogleHomeCallback } from './oauth-continuation';",'preserve login import');
index=replaceOnce(index,"import { dbSchemaHealth, dbRuntimeHealth, liffConfigDiagnose } from './runtime-diagnostics';","import { dbSchemaHealth, dbRuntimeHealth } from './runtime-diagnostics';",'liff diagnose import');
for(const importLine of [
  "import { webhook } from './line-webhook';\n",
  "import { convertOccurrence } from './recurring-occurrence';\n",
  "import { reorderApi } from './reorder-api';\n",
  "import { taskDelete } from './task-delete';\n",
  "import { taskNew, itemNew } from './new-entry-pages';\n",
]) index=replaceOnce(index,importLine,'',`moved import ${importLine.trim()}`);
index=replaceOnce(index,"import { dispatchPublicRoute } from './public-routes';","import { dispatchPublicRoute } from './public-routes';\nimport { dispatchContextPreludeRoute, dispatchContextFallbackRoute } from './exception-routes';",'dispatcher import anchor');
fs.writeFileSync(indexPath,index);

const moduleSource=`import { liffLogin, toggle } from './app';\nimport { preserveGoogleHomeLogin } from './oauth-continuation';\nimport { googleAuthorize } from './google-home';\nimport { googleTasksAuthorize } from './google-tasks';\nimport { googleCalendarAuthorize } from './google-calendar';\nimport { liffConfigDiagnose } from './runtime-diagnostics';\nimport { reorderApi } from './reorder-api';\nimport { webhook } from './line-webhook';\nimport { taskDelete } from './task-delete';\nimport { convertOccurrence } from './recurring-occurrence';\nimport { taskNew, itemNew } from './new-entry-pages';\nimport { DEFAULT_FAMILY_TIMEZONE, familyDate } from './timezone';\n\nfunction asDateOffset(days:number,timeZone=DEFAULT_FAMILY_TIMEZONE){const base=familyDate(timeZone),d=new Date(\`${'${base}'}T12:00:00Z\`);d.setUTCDate(d.getUTCDate()+days);return d.toISOString().slice(0,10);}\n\nasync function logout():Promise<Response>{\n  const headers=new Headers({'Location':'/login.php','Set-Cookie':'family_line_cf=; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=0'});\n  return new Response(null,{status:302,headers});\n}\n\nexport async function dispatchContextPreludeRoute(request:Request,context:any,env:Env,url:URL):Promise<Response|null>{\n  if(url.pathname==='/oauth/google/authorize') {\n    console.log(JSON.stringify({stage:'AUTHORIZE_RECEIVED',provider:'GOOGLE_HOME'}));\n    return await preserveGoogleHomeLogin(request,env,await googleAuthorize(request,context));\n  }\n  if(url.pathname==='/oauth/google-tasks/authorize') return await googleTasksAuthorize(request,context);\n  if(url.pathname==='/oauth/google-calendar/authorize') return await googleCalendarAuthorize(request,context);\n  if(url.pathname==='/app/api/liff_login.php'||url.pathname==='/app/api/liff_login') return await liffLogin(request,context);\n  return null;\n}\n\nexport async function dispatchContextFallbackRoute(request:Request,context:any,env:Env,url:URL):Promise<Response|null>{\n  if(url.pathname==='/app/api/liff_config_diagnose.php'||url.pathname==='/app/api/liff_config_diagnose') return await liffConfigDiagnose(env);\n  if(url.pathname==='/app/api/check.php'||url.pathname==='/app/api/check') return await toggle(request,context);\n  if(url.pathname==='/app/api/reorder.php'||url.pathname==='/app/api/reorder') return await reorderApi(request,context);\n  if(url.pathname==='/webhook'||url.pathname==='/app/api/webhook'||url.pathname==='/app/api/webhook.php') return await webhook(request,env);\n  if(url.pathname==='/logout.php'||url.pathname==='/logout') return await logout();\n  if(url.pathname==='/task/delete.php') return await taskDelete(request,context);\n  if(url.pathname==='/task/convert_occurrence.php') return await convertOccurrence(request,context);\n  if(url.pathname==='/task/new.php') return await taskNew(context,url.searchParams.get('date')||asDateOffset(0,String(context.member?.family_timezone||env.APP_TIMEZONE||DEFAULT_FAMILY_TIMEZONE)),url.searchParams.get('return')||'');\n  if(url.pathname==='/item/new.php') return await itemNew(context,url.searchParams.get('date')||asDateOffset(0,String(context.member?.family_timezone||env.APP_TIMEZONE||DEFAULT_FAMILY_TIMEZONE)),Number(url.searchParams.get('task_id')||0));\n  return null;\n}\n`;
fs.writeFileSync(modulePath,moduleSource);

const contract=`import fs from 'node:fs';\n\nconst index=fs.readFileSync('src/index.ts','utf8');\nconst routes=fs.readFileSync('src/exception-routes.ts','utf8');\n\nfor(const marker of [\n  "import { dispatchContextPreludeRoute, dispatchContextFallbackRoute } from './exception-routes';",\n  'const preludeResponse=await dispatchContextPreludeRoute(request,context,env,url);',\n  'const fallbackResponse=await dispatchContextFallbackRoute(request,context,env,url);',\n]) if(!index.includes(marker)) throw new Error(\`index dispatcher wiring missing: \${marker}\`);\n\nconst preludePos=index.indexOf('const preludeResponse=');\nconst apiPos=index.indexOf('const apiResponse=');\nconst pagePos=index.indexOf('const pageResponse=');\nconst fallbackPos=index.indexOf('const fallbackResponse=');\nif(!(preludePos>=0&&preludePos<apiPos&&apiPos<pagePos&&pagePos<fallbackPos)) throw new Error('exception dispatcher ordering changed');\n\nfor(const marker of [\n  "if(url.pathname==='/oauth/google/authorize')",\n  "if(url.pathname==='/oauth/google-tasks/authorize')",\n  "if(url.pathname==='/oauth/google-calendar/authorize')",\n  "if(url.pathname==='/app/api/liff_login.php'||url.pathname==='/app/api/liff_login')",\n  "if(url.pathname==='/app/api/liff_config_diagnose.php'||url.pathname==='/app/api/liff_config_diagnose')",\n  "if(url.pathname==='/app/api/check.php'||url.pathname==='/app/api/check')",\n  "if(url.pathname==='/logout.php'||url.pathname==='/logout')",\n  "if(url.pathname==='/task/new.php')",\n  "if(url.pathname==='/item/new.php')",\n]) {\n  if(index.includes(marker)) throw new Error(\`exception route leaked into index: \${marker}\`);\n  if(!routes.includes(marker)) throw new Error(\`exception route missing from dispatcher: \${marker}\`);\n}\nfor(const marker of [\n  "if(url.pathname==='/app/api/reorder.php'||url.pathname==='/app/api/reorder')",\n  "if(url.pathname==='/webhook'||url.pathname==='/app/api/webhook'||url.pathname==='/app/api/webhook.php')",\n  "if(url.pathname==='/task/delete.php')",\n  "if(url.pathname==='/task/convert_occurrence.php')",\n  "console.log(JSON.stringify({stage:'AUTHORIZE_RECEIVED',provider:'GOOGLE_HOME'}));",\n  "'Set-Cookie':'family_line_cf=; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=0'",\n  'preserveGoogleHomeLogin(request,env,await googleAuthorize(request,context))',\n]) if(!routes.includes(marker)) throw new Error(\`exception route behavior marker missing: \${marker}\`);\n\nconsole.log('exception route dispatchers contract ok');\n`;
fs.writeFileSync(contractPath,contract);

let manifest=fs.readFileSync(manifestPath,'utf8');
manifest=replaceOnce(manifest,"      ['new-entry-pages-modularity','node scripts/new-entry-pages-modularity-contract.mjs'],","      ['new-entry-pages-modularity','node scripts/new-entry-pages-modularity-contract.mjs'],\n      ['exception-route-dispatchers','node scripts/exception-route-dispatchers-contract.mjs'],",'regression manifest anchor');
fs.writeFileSync(manifestPath,manifest);

console.log('exception route dispatchers extraction applied');
