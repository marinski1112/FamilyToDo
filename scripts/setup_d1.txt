# D1 database is already bound in wrangler.jsonc.
# Apply migrations from the project root:
#   npx wrangler d1 migrations apply familytodo --remote
# Do not create or configure Hyperdrive for the current build.
